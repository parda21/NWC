Read `AGENTS.md` first: it has the repository map, the build and test commands (which need a GPU and which do not),
the rules for kernel changes and measurements, and the commit conventions. Nothing in this file overrides it.
